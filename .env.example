# Optional: URL of your Cloudflare Worker NSI proxy (see worker/nsi-proxy.js)
# Without this, the app falls back to corsproxy.io (rate-limited, for testing only)
# VITE_NSI_PROXY_URL=https://nsi-proxy.your-subdomain.workers.dev
